apiVersion: v1
kind: Service
metadata:
  name: {{ include "ouma-chart.fullname" . }}
  labels:
    {{- include "ouma-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ouma-chart.selectorLabels" . | nindent 4 }}
