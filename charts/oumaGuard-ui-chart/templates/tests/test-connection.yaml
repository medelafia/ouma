apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ouma-ui-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ouma-ui-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ouma-ui-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
