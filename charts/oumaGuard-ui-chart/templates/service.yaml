apiVersion: v1
kind: Service
metadata:
  name: {{ include "ouma-ui-chart.fullname" . }}
  labels:
    {{- include "ouma-ui-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ouma-ui-chart.selectorLabels" . | nindent 4 }}
